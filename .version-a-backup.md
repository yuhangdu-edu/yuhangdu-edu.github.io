# Version A — Liberia gallery snapshot (before box + shrink)

To restore, paste these blocks back into `assets/css/main.scss` replacing the
current `.field-gallery-*` rules.

```scss
/* Field research photo gallery slider */
.field-gallery-wrap {
  margin: 1.5em 0;
}

.field-gallery {
  display: flex;
  gap: 1em;
  overflow-x: scroll;
  scroll-behavior: smooth;
  -webkit-overflow-scrolling: touch;

  /* styled native scrollbar */
  scrollbar-width: auto;
  scrollbar-color: #999 #d8d8d8;

  &::-webkit-scrollbar { height: 14px; }
  &::-webkit-scrollbar-track { background: #d8d8d8; border-radius: 7px; }
  &::-webkit-scrollbar-thumb { background: #999; border-radius: 7px; }
  &::-webkit-scrollbar-thumb:hover { background: #777; }
}

.field-gallery__item {
  flex: 0 0 520px;
  display: flex;
  flex-direction: column;
  cursor: pointer;

  img {
    width: 100%;
    height: 380px;
    object-fit: cover;
    display: block;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.12);
    transition: transform 0.3s ease;
  }

  &:hover img {
    transform: scale(1.03);
  }
}

.field-gallery__caption {
  font-size: 0.92em;
  color: var(--global-text-color);
  text-align: left;
  padding: 0.6em 0.25em 0;
  min-height: 3em;
  line-height: 1.45;
}
```

Markdown gallery block in `_pages/field.md` was unchanged in this version.
